CREATE TABLE DATA_LOG
(
	ID INT NOT NULL AUTO_INCREMENT,
	Address VARCHAR(255),
	ApplianceName VARCHAR(255),
	RegisterName VARCHAR(255),
	Value INT,
	InsertTime TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	PRIMARY KEY (ID)
);
